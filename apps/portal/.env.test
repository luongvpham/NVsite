# Vitest chạy ở mode "test" — không đọc .env.development. Cần relative URL để MSW (chạy trên
# origin http://localhost mặc định của jsdom) intercept đúng path, xem .env.development để biết lý do.
VITE_API_BASE_URL=/
