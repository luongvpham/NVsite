/**
 * Re-export — nguồn sự thật thật sự nằm ở @vsite/builder-components/src/context.tsx.
 * Xem ghi chú trong file đó về lý do (tránh phụ thuộc vòng builder-components ↔ builder-renderer).
 */
export { RenderContextProvider, useRenderContext, type RenderContextValue } from '@vsite/builder-components';
