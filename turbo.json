{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "gen:api", "gen:registry"],
      "outputs": ["dist/**", ".output/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build", "gen:registry"]
    },
    "gen:api": {
      "cache": true,
      "inputs": ["$TURBO_ROOT$/contracts/openapi/**"],
      "outputs": ["src/**"]
    },
    "gen:registry": {
      "cache": true,
      "outputs": ["generated/**"]
    }
  }
}
